// Task 8.1
public static void main(String[] args) {
    String[] names = new String[]{"Gisbert", "Manuel", "Seba", "Jan", "Max"};
    int var3 = names.length;

    for(int var4 = 0; var4 < var3; ++var4) {
        String l = names[var4];
        System.out.println(l);
    }
}

// Task 8.2
public static void main(String[] args) {
    System.out.println("Enter your word to be checked here:");
    Scanner sc = new Scanner(System.in);
    String s = sc.nextLine();
    String word = s.toLowerCase();
    char[] eingArr = wort.toCharArray();
    char[] revArr = reverse(inputArr);
    System.out.println(revArr);

    if (Arrays.equals(inputArr, revArr)) {
        System.out.println("It is a palindrome");
    } else {
        System.out.println("It is not a palindrome");
    }
}

public static char[] reverse(char[] a) {
    char[] result = new char[a.length];
    int counter = 0;

    for(int i = a.length - 1; i >= 0; --i) {
        result[counter] = a[i];
        ++counter;
    } return result;
}

// Task 8.3
public static void main(String[] args) {
    System.out.println("Enter the desired size:");
    Scanner sc = new Scanner(System.in);
    int size = sc.nextInt();
    int[] list = new int[size];
    int max = 0;

    for(int i = 0; i < size; ++i) {
        System.out.println("Enter the number to add:");
        list[i] = sc.nextInt();
        if (list[i] > max) {
            max = list[i];
        }
        int var7 = list.length;

        for(int var8 = 0; var8 < var7; ++var8) {
            int elem = list[var8];
            System.out.println(elem);
        }
    } System.out.println("The largest number is:" + max);
}